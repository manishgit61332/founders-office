# Local client contract only. Copy to an untracked `.env.local` if a local adapter needs it.
FOUNDER_OFFICE_SUPABASE_URL=http://127.0.0.1:54321
FOUNDER_OFFICE_SUPABASE_PUBLISHABLE_KEY=

# Production values, OAuth client IDs/secrets, service-role keys, and signing keys
# belong in the approved deployment secret store, never in this repository.
